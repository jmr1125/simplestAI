#include "layer.h"
VvalT layer::getV(const VvalT &input) {
  matrix res;
  res = w * input;
  for (int i = 0; i < res.getn(); ++i) {
    res(i, 0) = (*(this->Func))(res(i, 0));
  }
  res=res+basis;
  return res.getvec();
}
